"""Test script for pagination — creates large collections."""

big_list = list(range(50))
big_dict = {f"key_{i}": i * 10 for i in range(30)}
small_list = [1, 2, 3]

# Breakpoint target
print("inspect here")  # line 8
print("done")
